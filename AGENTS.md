# AGENTS.md

## Workflow rule

- After completing each task (or each major work unit), update the todo list with `todowrite`: mark finished items as `completed`. Always keep the todo list in sync — never leave finished work marked as in-progress or pending.
